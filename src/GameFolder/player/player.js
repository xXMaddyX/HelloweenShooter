import Phaser from "phaser";

export default class Player {
    constructor(scene) {
        /**@type {Phaser.Scene} */
        this.scene = scene
    };

    //MAKE STATIC LATER!!!!!!!!!!!!
    loadSprites() {

    };

    //MAKE STATIC LATER!!!!!!!!!!!!
    initAnimations() {

    };

    create() {

    };

    update(time, delta) {

    };
};